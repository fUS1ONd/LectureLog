services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    volumes:
      - "./data:/app/data"

  bot:
    build:
      context: .
      dockerfile: Dockerfile.bot
    env_file: .env
    depends_on:
      - api
