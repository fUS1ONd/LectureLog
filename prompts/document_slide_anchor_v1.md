Выбери безопасную границу Markdown-блоков для слайда по evidence.

Не выбирай позицию внутри fenced code, списка или callout. Верни только JSON:
`{"slide_num": N, "block_index": I, "side": "before|after"}`. Если точного
семантического блока нет, не выдумывай anchor.

